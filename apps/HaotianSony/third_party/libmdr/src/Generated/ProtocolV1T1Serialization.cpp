/* This file is auto-generated by tooling/SerializationCodegen.cpp */
#include <mdr/ProtocolV1T1.hpp>

namespace mdr::v1::t1 {
    MDRResult<size_t> AsCapabilityAction::Write(const AsCapabilityAction& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.action, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.function, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> AsCapabilityAction::Read(const UInt8** ppSrcBuffer, AsCapabilityAction& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.action, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.function, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> Asm::Write(const Asm& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.id, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.step, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> Asm::Read(const UInt8** ppSrcBuffer, Asm& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.id, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.step, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> AsmParam::Write(const AsmParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmEffect, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> AsmParam::Read(const UInt8** ppSrcBuffer, AsmParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncAsmEffect, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmId, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> AssignableSettingsParam::Write(const AssignableSettingsParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AssignableSettingsPreset>::Write)(data.presets, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> AssignableSettingsParam::Read(const UInt8** ppSrcBuffer, AssignableSettingsParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AssignableSettingsPreset>::Read)(ppSrcBuffer, out.presets, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> AssignableSettingsStatus::Write(const AssignableSettingsStatus& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, (MDRPodArray<CommonStatus>::Write)(data.allStatus, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> AssignableSettingsStatus::Read(const UInt8** ppSrcBuffer, AssignableSettingsStatus& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, (MDRPodArray<CommonStatus>::Read)(ppSrcBuffer, out.allStatus, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> AtCommandParam::Write(const AtCommandParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.messageType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.commandLength, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.command2, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> AtCommandParam::Read(const UInt8** ppSrcBuffer, AtCommandParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.messageType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.commandLength, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Read)(ppSrcBuffer, out.command2, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> AutoPowerOffParam::Write(const AutoPowerOffParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.autoPowerOffParameterType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.activeElementId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.selectTimeElementId, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> AutoPowerOffParam::Read(const UInt8** ppSrcBuffer, AutoPowerOffParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.autoPowerOffParameterType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.activeElementId, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.selectTimeElementId, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> BatteryParam::Write(const BatteryParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.level, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.chargingStatus, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> BatteryParam::Read(const UInt8** ppSrcBuffer, BatteryParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.level, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.chargingStatus, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> ConnectionModeParam::Write(const ConnectionModeParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> ConnectionModeParam::Read(const UInt8** ppSrcBuffer, ConnectionModeParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> ControlByWearingParam::Write(const ControlByWearingParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> ControlByWearingParam::Read(const UInt8** ppSrcBuffer, ControlByWearingParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> CradleBatteryParam::Write(const CradleBatteryParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.level, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.chargingStatus, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> CradleBatteryParam::Read(const UInt8** ppSrcBuffer, CradleBatteryParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.level, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.chargingStatus, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> EbbParam::Write(const EbbParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.level, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> EbbParam::Read(const UInt8** ppSrcBuffer, EbbParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.level, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> EqBandInformation::Write(const EqBandInformation& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.infoType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.valueAsFrequency, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> EqBandInformation::Read(const UInt8** ppSrcBuffer, EqBandInformation& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.infoType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.valueAsFrequency, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> EqParam::Write(const EqParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Write)(data.bandSteps, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> EqParam::Read(const UInt8** ppSrcBuffer, EqParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.presetId, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Read)(ppSrcBuffer, out.bandSteps, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> EqPreset::Write(const EqPreset& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.name, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> EqPreset::Read(const UInt8** ppSrcBuffer, EqPreset& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.presetId, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Read)(ppSrcBuffer, out.name, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> FixedMessageParam::Write(const FixedMessageParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.messageType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.action, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> FixedMessageParam::Read(const UInt8** ppSrcBuffer, FixedMessageParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.messageType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.action, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> GetConciergeData::Serialize(const GetConciergeData& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.data, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<GetConciergeData> GetConciergeData::Deserialize(const UInt8* data, size_t maxSize)
    {
        GetConciergeData out{};
        MDR_TRY_SIZE(GetConciergeData, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(GetConciergeData, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(GetConciergeData, (MDRPrefixedString::Read)(&data, out.data, maxSize));
        MDR_TRY(GetConciergeData, Validate(out));
        return MDRResult<GetConciergeData>::Success(std::move(out));
    }
    MDRResult<size_t> GetSportsExParam_TrainingModeRequest::Write(const GetSportsExParam_TrainingModeRequest& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> GetSportsExParam_TrainingModeRequest::Read(const UInt8** ppSrcBuffer, GetSportsExParam_TrainingModeRequest& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> GsBooleanTypeValue::Write(const GsBooleanTypeValue& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> GsBooleanTypeValue::Read(const UInt8** ppSrcBuffer, GsBooleanTypeValue& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> GsListTypeValue::Write(const GsListTypeValue& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.currentElementIndex, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> GsListTypeValue::Read(const UInt8** ppSrcBuffer, GsListTypeValue& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.currentElementIndex, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> GsSettingInfo::Write(const GsSettingInfo& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.stringFormat, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.subject, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.summary, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> GsSettingInfo::Read(const UInt8** ppSrcBuffer, GsSettingInfo& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.stringFormat, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Read)(ppSrcBuffer, out.subject, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Read)(ppSrcBuffer, out.summary, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> KeepAliveLinkControlNotifiedParam::Write(const KeepAliveLinkControlNotifiedParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.status, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> KeepAliveLinkControlNotifiedParam::Read(const UInt8** ppSrcBuffer, KeepAliveLinkControlNotifiedParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.status, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> KeepAliveLinkControlSettingParam::Write(const KeepAliveLinkControlSettingParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.status, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.timeMin, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> KeepAliveLinkControlSettingParam::Read(const UInt8** ppSrcBuffer, KeepAliveLinkControlSettingParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.status, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.timeMin, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> LeftRightBatteryParam::Write(const LeftRightBatteryParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.leftLevel, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.rightLevel, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.leftChargingStatus, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.rightChargingStatus, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> LeftRightBatteryParam::Read(const UInt8** ppSrcBuffer, LeftRightBatteryParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.leftLevel, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.rightLevel, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.leftChargingStatus, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.rightChargingStatus, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> LeftRightConnectionStatusParam::Write(const LeftRightConnectionStatusParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.leftConnectionStatus, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.rightConnectionStatus, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> LeftRightConnectionStatusParam::Read(const UInt8** ppSrcBuffer, LeftRightConnectionStatusParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.leftConnectionStatus, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.rightConnectionStatus, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> NcAsmParam::Write(const NcAsmParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmEffect, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncValue, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> NcAsmParam::Read(const UInt8** ppSrcBuffer, NcAsmParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncAsmEffect, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncValue, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmId, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> NcParam::Write(const NcParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncSettingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncSettingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> NcParam::Read(const UInt8** ppSrcBuffer, NcParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncSettingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncSettingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> NotifyEqEbbParamEqParam::Serialize(const NotifyEqEbbParamEqParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Write)(data.bandSteps, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<NotifyEqEbbParamEqParam> NotifyEqEbbParamEqParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        NotifyEqEbbParamEqParam out{};
        MDR_TRY_SIZE(NotifyEqEbbParamEqParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(NotifyEqEbbParamEqParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(NotifyEqEbbParamEqParam, MDRPod::Read(&data, out.presetId, maxSize));
        MDR_TRY_SIZE(NotifyEqEbbParamEqParam, (MDRPodArray<UInt8>::Read)(&data, out.bandSteps, maxSize));
        MDR_TRY(NotifyEqEbbParamEqParam, Validate(out));
        return MDRResult<NotifyEqEbbParamEqParam>::Success(std::move(out));
    }
    MDRResult<size_t> NotifyLogParam::Serialize(const NotifyLogParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.logInquiredType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString16BE::Write)(data.data, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<NotifyLogParam> NotifyLogParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        NotifyLogParam out{};
        MDR_TRY_SIZE(NotifyLogParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(NotifyLogParam, MDRPod::Read(&data, out.logInquiredType, maxSize));
        MDR_TRY_SIZE(NotifyLogParam, (MDRPrefixedString16BE::Read)(&data, out.data, maxSize));
        MDR_TRY(NotifyLogParam, Validate(out));
        return MDRResult<NotifyLogParam>::Success(std::move(out));
    }
    MDRResult<size_t> NotifySystemParamAssignableSettingsParam::Serialize(const NotifySystemParamAssignableSettingsParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AssignableSettingsPreset>::Write)(data.presets, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<NotifySystemParamAssignableSettingsParam> NotifySystemParamAssignableSettingsParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        NotifySystemParamAssignableSettingsParam out{};
        MDR_TRY_SIZE(NotifySystemParamAssignableSettingsParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(NotifySystemParamAssignableSettingsParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(NotifySystemParamAssignableSettingsParam, (MDRPodArray<AssignableSettingsPreset>::Read)(&data, out.presets, maxSize));
        MDR_TRY(NotifySystemParamAssignableSettingsParam, Validate(out));
        return MDRResult<NotifySystemParamAssignableSettingsParam>::Success(std::move(out));
    }
    MDRResult<size_t> NotifySystemStatusAssignableSettingsStatus::Serialize(const NotifySystemStatusAssignableSettingsStatus& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<CommonStatus>::Write)(data.allStatus, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<NotifySystemStatusAssignableSettingsStatus> NotifySystemStatusAssignableSettingsStatus::Deserialize(const UInt8* data, size_t maxSize)
    {
        NotifySystemStatusAssignableSettingsStatus out{};
        MDR_TRY_SIZE(NotifySystemStatusAssignableSettingsStatus, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(NotifySystemStatusAssignableSettingsStatus, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(NotifySystemStatusAssignableSettingsStatus, (MDRPodArray<CommonStatus>::Read)(&data, out.allStatus, maxSize));
        MDR_TRY(NotifySystemStatusAssignableSettingsStatus, Validate(out));
        return MDRResult<NotifySystemStatusAssignableSettingsStatus>::Success(std::move(out));
    }
    MDRResult<size_t> Payload::Write(const Payload& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.commandType, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> Payload::Read(const UInt8** ppSrcBuffer, Payload& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.commandType, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> PlaybackControllerNotifyNameData::Write(const PlaybackControllerNotifyNameData& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.dataType, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> PlaybackControllerNotifyNameData::Read(const UInt8** ppSrcBuffer, PlaybackControllerNotifyNameData& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.dataType, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> PlaybackControllerVolumeData::Write(const PlaybackControllerVolumeData& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.dataType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.volumeValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> PlaybackControllerVolumeData::Read(const UInt8** ppSrcBuffer, PlaybackControllerVolumeData& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.dataType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.volumeValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> PlaybackName::Write(const PlaybackName& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.nameStatus, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.name, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> PlaybackName::Read(const UInt8** ppSrcBuffer, PlaybackName& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.nameStatus, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Read)(ppSrcBuffer, out.name, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> PowerSavingModeParam::Write(const PowerSavingModeParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> PowerSavingModeParam::Read(const UInt8** ppSrcBuffer, PowerSavingModeParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RawPayload::Write(const RawPayload& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Write)(data.bytes, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> RawPayload::Read(const UInt8** ppSrcBuffer, RawPayload& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Read)(ppSrcBuffer, out.bytes, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RetAudioCapability_AudioCapabilityBase::Write(const RetAudioCapability_AudioCapabilityBase& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> RetAudioCapability_AudioCapabilityBase::Read(const UInt8** ppSrcBuffer, RetAudioCapability_AudioCapabilityBase& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RetBluetoothDeviceInfo::Serialize(const RetBluetoothDeviceInfo& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.deviceInformation, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetBluetoothDeviceInfo> RetBluetoothDeviceInfo::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetBluetoothDeviceInfo out{};
        MDR_TRY_SIZE(RetBluetoothDeviceInfo, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetBluetoothDeviceInfo, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetBluetoothDeviceInfo, (MDRPrefixedString::Read)(&data, out.deviceInformation, maxSize));
        MDR_TRY(RetBluetoothDeviceInfo, Validate(out));
        return MDRResult<RetBluetoothDeviceInfo>::Success(std::move(out));
    }
    MDRResult<size_t> RetCapabilityInfo::Serialize(const RetCapabilityInfo& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.capabilityCounter, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.uniqueId, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetCapabilityInfo> RetCapabilityInfo::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetCapabilityInfo out{};
        MDR_TRY_SIZE(RetCapabilityInfo, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetCapabilityInfo, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetCapabilityInfo, MDRPod::Read(&data, out.capabilityCounter, maxSize));
        MDR_TRY_SIZE(RetCapabilityInfo, (MDRPrefixedString::Read)(&data, out.uniqueId, maxSize));
        MDR_TRY(RetCapabilityInfo, Validate(out));
        return MDRResult<RetCapabilityInfo>::Success(std::move(out));
    }
    MDRResult<size_t> RetConciergeData::Serialize(const RetConciergeData& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.data, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetConciergeData> RetConciergeData::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetConciergeData out{};
        MDR_TRY_SIZE(RetConciergeData, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetConciergeData, (MDRPrefixedString::Read)(&data, out.data, maxSize));
        MDR_TRY(RetConciergeData, Validate(out));
        return MDRResult<RetConciergeData>::Success(std::move(out));
    }
    MDRResult<size_t> RetDeviceInfo_DeviceInfoBase::Write(const RetDeviceInfo_DeviceInfoBase& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> RetDeviceInfo_DeviceInfoBase::Read(const UInt8** ppSrcBuffer, RetDeviceInfo_DeviceInfoBase& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RetEqEbbCapability_EqEbbCapabilityBase::Write(const RetEqEbbCapability_EqEbbCapabilityBase& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> RetEqEbbCapability_EqEbbCapabilityBase::Read(const UInt8** ppSrcBuffer, RetEqEbbCapability_EqEbbCapabilityBase& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RetEqEbbParamEqParam::Serialize(const RetEqEbbParamEqParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Write)(data.bandSteps, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetEqEbbParamEqParam> RetEqEbbParamEqParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetEqEbbParamEqParam out{};
        MDR_TRY_SIZE(RetEqEbbParamEqParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetEqEbbParamEqParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetEqEbbParamEqParam, MDRPod::Read(&data, out.presetId, maxSize));
        MDR_TRY_SIZE(RetEqEbbParamEqParam, (MDRPodArray<UInt8>::Read)(&data, out.bandSteps, maxSize));
        MDR_TRY(RetEqEbbParamEqParam, Validate(out));
        return MDRResult<RetEqEbbParamEqParam>::Success(std::move(out));
    }
    MDRResult<size_t> RetNcAsmCapability_NcAsmCapabilityBase::Write(const RetNcAsmCapability_NcAsmCapabilityBase& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> RetNcAsmCapability_NcAsmCapabilityBase::Read(const UInt8** ppSrcBuffer, RetNcAsmCapability_NcAsmCapabilityBase& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RetSupportFunction::Serialize(const RetSupportFunction& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<FunctionType>::Write)(data.supportFunctions, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSupportFunction> RetSupportFunction::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSupportFunction out{};
        MDR_TRY_SIZE(RetSupportFunction, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSupportFunction, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetSupportFunction, (MDRPodArray<FunctionType>::Read)(&data, out.supportFunctions, maxSize));
        MDR_TRY(RetSupportFunction, Validate(out));
        return MDRResult<RetSupportFunction>::Success(std::move(out));
    }
    MDRResult<size_t> RetSystemCapability_SystemCapabilityBase::Write(const RetSystemCapability_SystemCapabilityBase& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> RetSystemCapability_SystemCapabilityBase::Read(const UInt8** ppSrcBuffer, RetSystemCapability_SystemCapabilityBase& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RetSystemParamAssignableSettingsParam::Serialize(const RetSystemParamAssignableSettingsParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AssignableSettingsPreset>::Write)(data.presets, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSystemParamAssignableSettingsParam> RetSystemParamAssignableSettingsParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSystemParamAssignableSettingsParam out{};
        MDR_TRY_SIZE(RetSystemParamAssignableSettingsParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSystemParamAssignableSettingsParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetSystemParamAssignableSettingsParam, (MDRPodArray<AssignableSettingsPreset>::Read)(&data, out.presets, maxSize));
        MDR_TRY(RetSystemParamAssignableSettingsParam, Validate(out));
        return MDRResult<RetSystemParamAssignableSettingsParam>::Success(std::move(out));
    }
    MDRResult<size_t> RetSystemStatusAssignableSettingsStatus::Serialize(const RetSystemStatusAssignableSettingsStatus& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<CommonStatus>::Write)(data.allStatus, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSystemStatusAssignableSettingsStatus> RetSystemStatusAssignableSettingsStatus::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSystemStatusAssignableSettingsStatus out{};
        MDR_TRY_SIZE(RetSystemStatusAssignableSettingsStatus, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSystemStatusAssignableSettingsStatus, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetSystemStatusAssignableSettingsStatus, (MDRPodArray<CommonStatus>::Read)(&data, out.allStatus, maxSize));
        MDR_TRY(RetSystemStatusAssignableSettingsStatus, Validate(out));
        return MDRResult<RetSystemStatusAssignableSettingsStatus>::Success(std::move(out));
    }
    MDRResult<size_t> RetUpdateParamUpdateStringParamNSString::Serialize(const RetUpdateParamUpdateStringParamNSString& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.string, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetUpdateParamUpdateStringParamNSString> RetUpdateParamUpdateStringParamNSString::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetUpdateParamUpdateStringParamNSString out{};
        MDR_TRY_SIZE(RetUpdateParamUpdateStringParamNSString, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetUpdateParamUpdateStringParamNSString, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetUpdateParamUpdateStringParamNSString, (MDRPrefixedString::Read)(&data, out.string, maxSize));
        MDR_TRY(RetUpdateParamUpdateStringParamNSString, Validate(out));
        return MDRResult<RetUpdateParamUpdateStringParamNSString>::Success(std::move(out));
    }
    MDRResult<size_t> RetVptCapability_VptCapabilityBase::Write(const RetVptCapability_VptCapabilityBase& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> RetVptCapability_VptCapabilityBase::Read(const UInt8** ppSrcBuffer, RetVptCapability_VptCapabilityBase& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> SetEqEbbParamEqParam::Serialize(const SetEqEbbParamEqParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Write)(data.bandSteps, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetEqEbbParamEqParam> SetEqEbbParamEqParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetEqEbbParamEqParam out{};
        MDR_TRY_SIZE(SetEqEbbParamEqParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetEqEbbParamEqParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetEqEbbParamEqParam, MDRPod::Read(&data, out.presetId, maxSize));
        MDR_TRY_SIZE(SetEqEbbParamEqParam, (MDRPodArray<UInt8>::Read)(&data, out.bandSteps, maxSize));
        MDR_TRY(SetEqEbbParamEqParam, Validate(out));
        return MDRResult<SetEqEbbParamEqParam>::Success(std::move(out));
    }
    MDRResult<size_t> SetSystemExParamAssignableSettingsParam::Serialize(const SetSystemExParamAssignableSettingsParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AssignableSettingsPreset>::Write)(data.presets, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSystemExParamAssignableSettingsParam> SetSystemExParamAssignableSettingsParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSystemExParamAssignableSettingsParam out{};
        MDR_TRY_SIZE(SetSystemExParamAssignableSettingsParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSystemExParamAssignableSettingsParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSystemExParamAssignableSettingsParam, (MDRPodArray<AssignableSettingsPreset>::Read)(&data, out.presets, maxSize));
        MDR_TRY(SetSystemExParamAssignableSettingsParam, Validate(out));
        return MDRResult<SetSystemExParamAssignableSettingsParam>::Success(std::move(out));
    }
    MDRResult<size_t> SetSystemExParamRetSystemCapability_AutoPowerOffCapability::Serialize(const SetSystemExParamRetSystemCapability_AutoPowerOffCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AutoPowerOffElementId>::Write)(data.candidateElements, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSystemExParamRetSystemCapability_AutoPowerOffCapability> SetSystemExParamRetSystemCapability_AutoPowerOffCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSystemExParamRetSystemCapability_AutoPowerOffCapability out{};
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_AutoPowerOffCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_AutoPowerOffCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_AutoPowerOffCapability, (MDRPodArray<AutoPowerOffElementId>::Read)(&data, out.candidateElements, maxSize));
        MDR_TRY(SetSystemExParamRetSystemCapability_AutoPowerOffCapability, Validate(out));
        return MDRResult<SetSystemExParamRetSystemCapability_AutoPowerOffCapability>::Success(std::move(out));
    }
    MDRResult<size_t> SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1::Serialize(const SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.previewType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.detailSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.detectionSensitivityType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.voiceFocusType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.modeOutTimeType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Write)(data.modeTimeOutValues, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1> SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1 out{};
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.settingType, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.previewType, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.detailSettingType, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.detectionSensitivityType, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.voiceFocusType, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.modeOutTimeType, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, (MDRPodArray<UInt8>::Read)(&data, out.modeTimeOutValues, maxSize));
        MDR_TRY(SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, Validate(out));
        return MDRResult<SetSystemExParamRetSystemCapability_SmartTalkingModeCapabilityVariant1>::Success(std::move(out));
    }
    MDRResult<size_t> SetSystemParamAssignableSettingsParam::Serialize(const SetSystemParamAssignableSettingsParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AssignableSettingsPreset>::Write)(data.presets, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSystemParamAssignableSettingsParam> SetSystemParamAssignableSettingsParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSystemParamAssignableSettingsParam out{};
        MDR_TRY_SIZE(SetSystemParamAssignableSettingsParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSystemParamAssignableSettingsParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSystemParamAssignableSettingsParam, (MDRPodArray<AssignableSettingsPreset>::Read)(&data, out.presets, maxSize));
        MDR_TRY(SetSystemParamAssignableSettingsParam, Validate(out));
        return MDRResult<SetSystemParamAssignableSettingsParam>::Success(std::move(out));
    }
    MDRResult<size_t> SetSystemParamRetSystemCapability_AutoPowerOffCapability::Serialize(const SetSystemParamRetSystemCapability_AutoPowerOffCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AutoPowerOffElementId>::Write)(data.candidateElements, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSystemParamRetSystemCapability_AutoPowerOffCapability> SetSystemParamRetSystemCapability_AutoPowerOffCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSystemParamRetSystemCapability_AutoPowerOffCapability out{};
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_AutoPowerOffCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_AutoPowerOffCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_AutoPowerOffCapability, (MDRPodArray<AutoPowerOffElementId>::Read)(&data, out.candidateElements, maxSize));
        MDR_TRY(SetSystemParamRetSystemCapability_AutoPowerOffCapability, Validate(out));
        return MDRResult<SetSystemParamRetSystemCapability_AutoPowerOffCapability>::Success(std::move(out));
    }
    MDRResult<size_t> SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1::Serialize(const SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.previewType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.detailSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.detectionSensitivityType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.voiceFocusType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.modeOutTimeType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Write)(data.modeTimeOutValues, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1> SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1 out{};
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.settingType, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.previewType, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.detailSettingType, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.detectionSensitivityType, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.voiceFocusType, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.modeOutTimeType, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, (MDRPodArray<UInt8>::Read)(&data, out.modeTimeOutValues, maxSize));
        MDR_TRY(SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1, Validate(out));
        return MDRResult<SetSystemParamRetSystemCapability_SmartTalkingModeCapabilityVariant1>::Success(std::move(out));
    }
    MDRResult<size_t> SetVptParamVptPreset::Serialize(const SetVptParamVptPreset& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.name, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetVptParamVptPreset> SetVptParamVptPreset::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetVptParamVptPreset out{};
        MDR_TRY_SIZE(SetVptParamVptPreset, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetVptParamVptPreset, MDRPod::Read(&data, out.presetId, maxSize));
        MDR_TRY_SIZE(SetVptParamVptPreset, (MDRPrefixedString::Read)(&data, out.name, maxSize));
        MDR_TRY(SetVptParamVptPreset, Validate(out));
        return MDRResult<SetVptParamVptPreset>::Success(std::move(out));
    }
    MDRResult<size_t> SmartTalkingModeExParamType1Param::Write(const SmartTalkingModeExParamType1Param& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.detailSettingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.devectionSensitivity, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.voiceFocus, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.modeOutTime, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> SmartTalkingModeExParamType1Param::Read(const UInt8** ppSrcBuffer, SmartTalkingModeExParamType1Param& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.detailSettingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.devectionSensitivity, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.voiceFocus, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.modeOutTime, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> SmartTalkingModeExType1Param::Write(const SmartTalkingModeExType1Param& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.devectionSensitivity, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.voiceFocus, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.modeOutTime, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> SmartTalkingModeExType1Param::Read(const UInt8** ppSrcBuffer, SmartTalkingModeExType1Param& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.devectionSensitivity, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.voiceFocus, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.modeOutTime, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> SmartTalkingModeRetParam::Write(const SmartTalkingModeRetParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> SmartTalkingModeRetParam::Read(const UInt8** ppSrcBuffer, SmartTalkingModeRetParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> SmartTalkingModeSetNtfyParam::Write(const SmartTalkingModeSetNtfyParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.parameterType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> SmartTalkingModeSetNtfyParam::Read(const UInt8** ppSrcBuffer, SmartTalkingModeSetNtfyParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.parameterType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> SmartTalkingModeStatus::Write(const SmartTalkingModeStatus& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.status, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.effectStatus, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> SmartTalkingModeStatus::Read(const UInt8** ppSrcBuffer, SmartTalkingModeStatus& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.status, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.effectStatus, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> SoundPositionParam::Write(const SoundPositionParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> SoundPositionParam::Read(const UInt8** ppSrcBuffer, SoundPositionParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.presetId, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> SpecificInformationType::Write(const SpecificInformationType& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.code, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> SpecificInformationType::Read(const UInt8** ppSrcBuffer, SpecificInformationType& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.code, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> SystemStatus::Write(const SystemStatus& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.status, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> SystemStatus::Read(const UInt8** ppSrcBuffer, SystemStatus& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.status, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeCapability::Write(const TrainingModeCapability& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.availableEffectType, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeCapability::Read(const UInt8** ppSrcBuffer, TrainingModeCapability& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.availableEffectType, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeExAsmParam::Write(const TrainingModeExAsmParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmEffect, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeExAsmParam::Read(const UInt8** ppSrcBuffer, TrainingModeExAsmParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncAsmEffect, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmId, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeExEqParam::Write(const TrainingModeExEqParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Write)(data.bandSteps, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeExEqParam::Read(const UInt8** ppSrcBuffer, TrainingModeExEqParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.presetId, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Read)(ppSrcBuffer, out.bandSteps, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeExNcAsmParam::Write(const TrainingModeExNcAsmParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmEffect, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncValue, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeExNcAsmParam::Read(const UInt8** ppSrcBuffer, TrainingModeExNcAsmParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncAsmEffect, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncValue, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmId, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeParam::Write(const TrainingModeParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeParam::Read(const UInt8** ppSrcBuffer, TrainingModeParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeStatus::Write(const TrainingModeStatus& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.status, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeStatus::Read(const UInt8** ppSrcBuffer, TrainingModeStatus& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.status, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> UpdateBatteryPowerThresholdForInterruptiongUpdtParam::Write(const UpdateBatteryPowerThresholdForInterruptiongUpdtParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.threshold, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> UpdateBatteryPowerThresholdForInterruptiongUpdtParam::Read(const UInt8** ppSrcBuffer, UpdateBatteryPowerThresholdForInterruptiongUpdtParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.threshold, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> UpdateBatteryPowerThresholdParam::Write(const UpdateBatteryPowerThresholdParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.threshold, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> UpdateBatteryPowerThresholdParam::Read(const UInt8** ppSrcBuffer, UpdateBatteryPowerThresholdParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.threshold, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> UpdateBleTxPowerParam::Write(const UpdateBleTxPowerParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.bleTxPower, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> UpdateBleTxPowerParam::Read(const UInt8** ppSrcBuffer, UpdateBleTxPowerParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.bleTxPower, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> UpdateMethodParam::Write(const UpdateMethodParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.value, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> UpdateMethodParam::Read(const UInt8** ppSrcBuffer, UpdateMethodParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.value, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> UpdateStringParamNSString::Write(const UpdateStringParamNSString& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.string, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> UpdateStringParamNSString::Read(const UInt8** ppSrcBuffer, UpdateStringParamNSString& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Read)(ppSrcBuffer, out.string, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> UpdateStringParamUpdateInquiredType::Write(const UpdateStringParamUpdateInquiredType& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> UpdateStringParamUpdateInquiredType::Read(const UInt8** ppSrcBuffer, UpdateStringParamUpdateInquiredType& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> UpscalingParam::Write(const UpscalingParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> UpscalingParam::Read(const UInt8** ppSrcBuffer, UpscalingParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> VibratorAlertNotificationParam::Write(const VibratorAlertNotificationParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.vibrationType, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> VibratorAlertNotificationParam::Read(const UInt8** ppSrcBuffer, VibratorAlertNotificationParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.vibrationType, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> VibratorParam::Write(const VibratorParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingValue, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> VibratorParam::Read(const UInt8** ppSrcBuffer, VibratorParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.settingValue, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> VptParam::Write(const VptParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> VptParam::Read(const UInt8** ppSrcBuffer, VptParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.presetId, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> VptPreset::Write(const VptPreset& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.presetId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.name, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> VptPreset::Read(const UInt8** ppSrcBuffer, VptPreset& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.presetId, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Read)(ppSrcBuffer, out.name, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> AsCapabilityPreset::Write(const AsCapabilityPreset& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.preset, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AsCapabilityAction>::Write)(data.actions, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> AsCapabilityPreset::Read(const UInt8** ppSrcBuffer, AsCapabilityPreset& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.preset, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AsCapabilityAction>::Read)(ppSrcBuffer, out.actions, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> EqExtendedInfo::Write(const EqExtendedInfo& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<EqBandInformation>::Write)(data.bandInfos, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> EqExtendedInfo::Read(const UInt8** ppSrcBuffer, EqExtendedInfo& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.type, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<EqBandInformation>::Read)(ppSrcBuffer, out.bandInfos, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> GetSportsExParam::Serialize(const GetSportsExParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (GetSportsExParam_TrainingModeRequest::Write)(data.getSportsExParam_TrainingModeRequest, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<GetSportsExParam> GetSportsExParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        GetSportsExParam out{};
        MDR_TRY_SIZE(GetSportsExParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(GetSportsExParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(GetSportsExParam, (GetSportsExParam_TrainingModeRequest::Read)(&data, out.getSportsExParam_TrainingModeRequest, maxSize));
        MDR_TRY(GetSportsExParam, Validate(out));
        return MDRResult<GetSportsExParam>::Success(std::move(out));
    }
    MDRResult<size_t> GsCandidateElementList::Write(const GsCandidateElementList& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, (MDRArray<GsSettingInfo>::Write)(data.elements, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> GsCandidateElementList::Read(const UInt8** ppSrcBuffer, GsCandidateElementList& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, (MDRArray<GsSettingInfo>::Read)(ppSrcBuffer, out.elements, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> NotifySportsExParamTrainingModeExParam::Serialize(const NotifySportsExParamTrainingModeExParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.parameterType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmEffect, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.value4, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmId, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.value7, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExNcAsmParam::Write)(data.ncAsmParam, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExAsmParam::Write)(data.asmParam, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExEqParam::Write)(data.eqParam, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<NotifySportsExParamTrainingModeExParam> NotifySportsExParamTrainingModeExParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        NotifySportsExParamTrainingModeExParam out{};
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, MDRPod::Read(&data, out.parameterType, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, MDRPod::Read(&data, out.ncAsmEffect, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, MDRPod::Read(&data, out.ncAsmSettingType, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, MDRPod::Read(&data, out.value4, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, MDRPod::Read(&data, out.asmSettingType, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, MDRPod::Read(&data, out.asmId, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, MDRPod::Read(&data, out.value7, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, (TrainingModeExNcAsmParam::Read)(&data, out.ncAsmParam, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, (TrainingModeExAsmParam::Read)(&data, out.asmParam, maxSize));
        MDR_TRY_SIZE(NotifySportsExParamTrainingModeExParam, (TrainingModeExEqParam::Read)(&data, out.eqParam, maxSize));
        MDR_TRY(NotifySportsExParamTrainingModeExParam, Validate(out));
        return MDRResult<NotifySportsExParamTrainingModeExParam>::Success(std::move(out));
    }
    MDRResult<size_t> NotifySportsStatusTrainingModeStatus::Serialize(const NotifySportsStatusTrainingModeStatus& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeStatus::Write)(data.trainingStatus, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<NotifySportsStatusTrainingModeStatus> NotifySportsStatusTrainingModeStatus::Deserialize(const UInt8* data, size_t maxSize)
    {
        NotifySportsStatusTrainingModeStatus out{};
        MDR_TRY_SIZE(NotifySportsStatusTrainingModeStatus, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(NotifySportsStatusTrainingModeStatus, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(NotifySportsStatusTrainingModeStatus, (TrainingModeStatus::Read)(&data, out.trainingStatus, maxSize));
        MDR_TRY(NotifySportsStatusTrainingModeStatus, Validate(out));
        return MDRResult<NotifySportsStatusTrainingModeStatus>::Success(std::move(out));
    }
    MDRResult<size_t> PlaybackControllerNameData::Write(const PlaybackControllerNameData& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.dataType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (PlaybackName::Write)(data.playbackName, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> PlaybackControllerNameData::Read(const UInt8** ppSrcBuffer, PlaybackControllerNameData& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.dataType, maxSize));
        MDR_TRY_SIZE(size_t, (PlaybackName::Read)(ppSrcBuffer, out.playbackName, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RetAudioCapability::Serialize(const RetAudioCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetAudioCapability_AudioCapabilityBase::Write)(data.capability, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetAudioCapability> RetAudioCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetAudioCapability out{};
        MDR_TRY_SIZE(RetAudioCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetAudioCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetAudioCapability, (RetAudioCapability_AudioCapabilityBase::Read)(&data, out.capability, maxSize));
        MDR_TRY(RetAudioCapability, Validate(out));
        return MDRResult<RetAudioCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetDeviceInfo::Serialize(const RetDeviceInfo& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetDeviceInfo_DeviceInfoBase::Write)(data.deviceInfo, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetDeviceInfo> RetDeviceInfo::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetDeviceInfo out{};
        MDR_TRY_SIZE(RetDeviceInfo, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetDeviceInfo, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetDeviceInfo, (RetDeviceInfo_DeviceInfoBase::Read)(&data, out.deviceInfo, maxSize));
        MDR_TRY(RetDeviceInfo, Validate(out));
        return MDRResult<RetDeviceInfo>::Success(std::move(out));
    }
    MDRResult<size_t> RetDeviceInfo_DeviceInfoFwVersion::Serialize(const RetDeviceInfo_DeviceInfoFwVersion& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetDeviceInfo_DeviceInfoBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.fwVersion, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetDeviceInfo_DeviceInfoFwVersion> RetDeviceInfo_DeviceInfoFwVersion::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetDeviceInfo_DeviceInfoFwVersion out{};
        MDR_TRY_SIZE(RetDeviceInfo_DeviceInfoFwVersion, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetDeviceInfo_DeviceInfoFwVersion, (RetDeviceInfo_DeviceInfoBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetDeviceInfo_DeviceInfoFwVersion, (MDRPrefixedString::Read)(&data, out.fwVersion, maxSize));
        MDR_TRY(RetDeviceInfo_DeviceInfoFwVersion, Validate(out));
        return MDRResult<RetDeviceInfo_DeviceInfoFwVersion>::Success(std::move(out));
    }
    MDRResult<size_t> RetDeviceInfo_DeviceInfoInstructionGuide::Serialize(const RetDeviceInfo_DeviceInfoInstructionGuide& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetDeviceInfo_DeviceInfoBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<GuidanceCategory>::Write)(data.guidanceCategories, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetDeviceInfo_DeviceInfoInstructionGuide> RetDeviceInfo_DeviceInfoInstructionGuide::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetDeviceInfo_DeviceInfoInstructionGuide out{};
        MDR_TRY_SIZE(RetDeviceInfo_DeviceInfoInstructionGuide, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetDeviceInfo_DeviceInfoInstructionGuide, (RetDeviceInfo_DeviceInfoBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetDeviceInfo_DeviceInfoInstructionGuide, (MDRPodArray<GuidanceCategory>::Read)(&data, out.guidanceCategories, maxSize));
        MDR_TRY(RetDeviceInfo_DeviceInfoInstructionGuide, Validate(out));
        return MDRResult<RetDeviceInfo_DeviceInfoInstructionGuide>::Success(std::move(out));
    }
    MDRResult<size_t> RetDeviceInfo_DeviceInfoModelName::Serialize(const RetDeviceInfo_DeviceInfoModelName& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetDeviceInfo_DeviceInfoBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPrefixedString::Write)(data.modelName, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetDeviceInfo_DeviceInfoModelName> RetDeviceInfo_DeviceInfoModelName::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetDeviceInfo_DeviceInfoModelName out{};
        MDR_TRY_SIZE(RetDeviceInfo_DeviceInfoModelName, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetDeviceInfo_DeviceInfoModelName, (RetDeviceInfo_DeviceInfoBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetDeviceInfo_DeviceInfoModelName, (MDRPrefixedString::Read)(&data, out.modelName, maxSize));
        MDR_TRY(RetDeviceInfo_DeviceInfoModelName, Validate(out));
        return MDRResult<RetDeviceInfo_DeviceInfoModelName>::Success(std::move(out));
    }
    MDRResult<size_t> RetEqEbbCapability::Serialize(const RetEqEbbCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetEqEbbCapability_EqEbbCapabilityBase::Write)(data.eqEbbCapability, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetEqEbbCapability> RetEqEbbCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetEqEbbCapability out{};
        MDR_TRY_SIZE(RetEqEbbCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetEqEbbCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetEqEbbCapability, (RetEqEbbCapability_EqEbbCapabilityBase::Read)(&data, out.eqEbbCapability, maxSize));
        MDR_TRY(RetEqEbbCapability, Validate(out));
        return MDRResult<RetEqEbbCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetEqEbbCapability_EqCapability::Serialize(const RetEqEbbCapability_EqCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetEqEbbCapability_EqEbbCapabilityBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.band, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.step, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRArray<EqPreset>::Write)(data.presetList, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetEqEbbCapability_EqCapability> RetEqEbbCapability_EqCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetEqEbbCapability_EqCapability out{};
        MDR_TRY_SIZE(RetEqEbbCapability_EqCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetEqEbbCapability_EqCapability, (RetEqEbbCapability_EqEbbCapabilityBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetEqEbbCapability_EqCapability, MDRPod::Read(&data, out.band, maxSize));
        MDR_TRY_SIZE(RetEqEbbCapability_EqCapability, MDRPod::Read(&data, out.step, maxSize));
        MDR_TRY_SIZE(RetEqEbbCapability_EqCapability, (MDRArray<EqPreset>::Read)(&data, out.presetList, maxSize));
        MDR_TRY(RetEqEbbCapability_EqCapability, Validate(out));
        return MDRResult<RetEqEbbCapability_EqCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetEqEbbExtendedInfoEqExtendedInfo::Serialize(const RetEqEbbExtendedInfoEqExtendedInfo& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<EqBandInformation>::Write)(data.bandInfos, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetEqEbbExtendedInfoEqExtendedInfo> RetEqEbbExtendedInfoEqExtendedInfo::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetEqEbbExtendedInfoEqExtendedInfo out{};
        MDR_TRY_SIZE(RetEqEbbExtendedInfoEqExtendedInfo, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetEqEbbExtendedInfoEqExtendedInfo, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetEqEbbExtendedInfoEqExtendedInfo, (MDRPodArray<EqBandInformation>::Read)(&data, out.bandInfos, maxSize));
        MDR_TRY(RetEqEbbExtendedInfoEqExtendedInfo, Validate(out));
        return MDRResult<RetEqEbbExtendedInfoEqExtendedInfo>::Success(std::move(out));
    }
    MDRResult<size_t> RetGsCapabilityGsSettingInfo::Serialize(const RetGsCapabilityGsSettingInfo& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (GsSettingInfo::Write)(data.title, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetGsCapabilityGsSettingInfo> RetGsCapabilityGsSettingInfo::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetGsCapabilityGsSettingInfo out{};
        MDR_TRY_SIZE(RetGsCapabilityGsSettingInfo, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetGsCapabilityGsSettingInfo, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetGsCapabilityGsSettingInfo, (GsSettingInfo::Read)(&data, out.title, maxSize));
        MDR_TRY_SIZE(RetGsCapabilityGsSettingInfo, MDRPod::Read(&data, out.settingType, maxSize));
        MDR_TRY(RetGsCapabilityGsSettingInfo, Validate(out));
        return MDRResult<RetGsCapabilityGsSettingInfo>::Success(std::move(out));
    }
    MDRResult<size_t> RetNcAsmCapability::Serialize(const RetNcAsmCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetNcAsmCapability_NcAsmCapabilityBase::Write)(data.ncAsmCapability, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetNcAsmCapability> RetNcAsmCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetNcAsmCapability out{};
        MDR_TRY_SIZE(RetNcAsmCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability, (RetNcAsmCapability_NcAsmCapabilityBase::Read)(&data, out.ncAsmCapability, maxSize));
        MDR_TRY(RetNcAsmCapability, Validate(out));
        return MDRResult<RetNcAsmCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetNcAsmCapability_AsmCapability::Serialize(const RetNcAsmCapability_AsmCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetNcAsmCapability_NcAsmCapabilityBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<Asm>::Write)(data.asmList, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetNcAsmCapability_AsmCapability> RetNcAsmCapability_AsmCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetNcAsmCapability_AsmCapability out{};
        MDR_TRY_SIZE(RetNcAsmCapability_AsmCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability_AsmCapability, (RetNcAsmCapability_NcAsmCapabilityBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability_AsmCapability, MDRPod::Read(&data, out.asmSettingType, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability_AsmCapability, (MDRPodArray<Asm>::Read)(&data, out.asmList, maxSize));
        MDR_TRY(RetNcAsmCapability_AsmCapability, Validate(out));
        return MDRResult<RetNcAsmCapability_AsmCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetNcAsmCapability_NcAsmCapability::Serialize(const RetNcAsmCapability_NcAsmCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetNcAsmCapability_NcAsmCapabilityBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncStep, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<Asm>::Write)(data.asmList, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetNcAsmCapability_NcAsmCapability> RetNcAsmCapability_NcAsmCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetNcAsmCapability_NcAsmCapability out{};
        MDR_TRY_SIZE(RetNcAsmCapability_NcAsmCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability_NcAsmCapability, (RetNcAsmCapability_NcAsmCapabilityBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability_NcAsmCapability, MDRPod::Read(&data, out.ncSettingType, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability_NcAsmCapability, MDRPod::Read(&data, out.ncStep, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability_NcAsmCapability, MDRPod::Read(&data, out.asmSettingType, maxSize));
        MDR_TRY_SIZE(RetNcAsmCapability_NcAsmCapability, (MDRPodArray<Asm>::Read)(&data, out.asmList, maxSize));
        MDR_TRY(RetNcAsmCapability_NcAsmCapability, Validate(out));
        return MDRResult<RetNcAsmCapability_NcAsmCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetPlayParamPlaybackControllerNameData::Serialize(const RetPlayParamPlaybackControllerNameData& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.playInquiredType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.dataType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (PlaybackName::Write)(data.playbackName, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetPlayParamPlaybackControllerNameData> RetPlayParamPlaybackControllerNameData::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetPlayParamPlaybackControllerNameData out{};
        MDR_TRY_SIZE(RetPlayParamPlaybackControllerNameData, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetPlayParamPlaybackControllerNameData, MDRPod::Read(&data, out.playInquiredType, maxSize));
        MDR_TRY_SIZE(RetPlayParamPlaybackControllerNameData, MDRPod::Read(&data, out.dataType, maxSize));
        MDR_TRY_SIZE(RetPlayParamPlaybackControllerNameData, (PlaybackName::Read)(&data, out.playbackName, maxSize));
        MDR_TRY(RetPlayParamPlaybackControllerNameData, Validate(out));
        return MDRResult<RetPlayParamPlaybackControllerNameData>::Success(std::move(out));
    }
    MDRResult<size_t> RetSportsCapability::Serialize(const RetSportsCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeCapability::Write)(data.trainingCapability, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSportsCapability> RetSportsCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSportsCapability out{};
        MDR_TRY_SIZE(RetSportsCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSportsCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetSportsCapability, (TrainingModeCapability::Read)(&data, out.trainingCapability, maxSize));
        MDR_TRY(RetSportsCapability, Validate(out));
        return MDRResult<RetSportsCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetSportsExParamTrainingModeExParam::Serialize(const RetSportsExParamTrainingModeExParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.parameterType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmEffect, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.value4, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmId, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.value7, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExNcAsmParam::Write)(data.ncAsmParam, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExAsmParam::Write)(data.asmParam, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExEqParam::Write)(data.eqParam, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSportsExParamTrainingModeExParam> RetSportsExParamTrainingModeExParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSportsExParamTrainingModeExParam out{};
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, MDRPod::Read(&data, out.parameterType, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, MDRPod::Read(&data, out.ncAsmEffect, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, MDRPod::Read(&data, out.ncAsmSettingType, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, MDRPod::Read(&data, out.value4, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, MDRPod::Read(&data, out.asmSettingType, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, MDRPod::Read(&data, out.asmId, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, MDRPod::Read(&data, out.value7, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, (TrainingModeExNcAsmParam::Read)(&data, out.ncAsmParam, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, (TrainingModeExAsmParam::Read)(&data, out.asmParam, maxSize));
        MDR_TRY_SIZE(RetSportsExParamTrainingModeExParam, (TrainingModeExEqParam::Read)(&data, out.eqParam, maxSize));
        MDR_TRY(RetSportsExParamTrainingModeExParam, Validate(out));
        return MDRResult<RetSportsExParamTrainingModeExParam>::Success(std::move(out));
    }
    MDRResult<size_t> RetSportsStatusTrainingModeStatus::Serialize(const RetSportsStatusTrainingModeStatus& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeStatus::Write)(data.trainingStatus, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSportsStatusTrainingModeStatus> RetSportsStatusTrainingModeStatus::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSportsStatusTrainingModeStatus out{};
        MDR_TRY_SIZE(RetSportsStatusTrainingModeStatus, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSportsStatusTrainingModeStatus, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetSportsStatusTrainingModeStatus, (TrainingModeStatus::Read)(&data, out.trainingStatus, maxSize));
        MDR_TRY(RetSportsStatusTrainingModeStatus, Validate(out));
        return MDRResult<RetSportsStatusTrainingModeStatus>::Success(std::move(out));
    }
    MDRResult<size_t> RetSystemCapability::Serialize(const RetSystemCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetSystemCapability_SystemCapabilityBase::Write)(data.capability, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSystemCapability> RetSystemCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSystemCapability out{};
        MDR_TRY_SIZE(RetSystemCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSystemCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetSystemCapability, (RetSystemCapability_SystemCapabilityBase::Read)(&data, out.capability, maxSize));
        MDR_TRY(RetSystemCapability, Validate(out));
        return MDRResult<RetSystemCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetSystemCapability_AutoPowerOffCapability::Serialize(const RetSystemCapability_AutoPowerOffCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetSystemCapability_SystemCapabilityBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<AutoPowerOffElementId>::Write)(data.candidateElements, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSystemCapability_AutoPowerOffCapability> RetSystemCapability_AutoPowerOffCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSystemCapability_AutoPowerOffCapability out{};
        MDR_TRY_SIZE(RetSystemCapability_AutoPowerOffCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_AutoPowerOffCapability, (RetSystemCapability_SystemCapabilityBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_AutoPowerOffCapability, (MDRPodArray<AutoPowerOffElementId>::Read)(&data, out.candidateElements, maxSize));
        MDR_TRY(RetSystemCapability_AutoPowerOffCapability, Validate(out));
        return MDRResult<RetSystemCapability_AutoPowerOffCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetSystemCapability_SmartTalkingModeCapabilityVariant1::Serialize(const RetSystemCapability_SmartTalkingModeCapabilityVariant1& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetSystemCapability_SystemCapabilityBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.previewType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.detailSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.detectionSensitivityType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.voiceFocusType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.modeOutTimeType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<UInt8>::Write)(data.modeTimeOutValues, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSystemCapability_SmartTalkingModeCapabilityVariant1> RetSystemCapability_SmartTalkingModeCapabilityVariant1::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSystemCapability_SmartTalkingModeCapabilityVariant1 out{};
        MDR_TRY_SIZE(RetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_SmartTalkingModeCapabilityVariant1, (RetSystemCapability_SystemCapabilityBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.settingType, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.previewType, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.detailSettingType, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.detectionSensitivityType, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.voiceFocusType, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_SmartTalkingModeCapabilityVariant1, MDRPod::Read(&data, out.modeOutTimeType, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_SmartTalkingModeCapabilityVariant1, (MDRPodArray<UInt8>::Read)(&data, out.modeTimeOutValues, maxSize));
        MDR_TRY(RetSystemCapability_SmartTalkingModeCapabilityVariant1, Validate(out));
        return MDRResult<RetSystemCapability_SmartTalkingModeCapabilityVariant1>::Success(std::move(out));
    }
    MDRResult<size_t> RetVptCapability::Serialize(const RetVptCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetVptCapability_VptCapabilityBase::Write)(data.capability, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetVptCapability> RetVptCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetVptCapability out{};
        MDR_TRY_SIZE(RetVptCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetVptCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetVptCapability, (RetVptCapability_VptCapabilityBase::Read)(&data, out.capability, maxSize));
        MDR_TRY(RetVptCapability, Validate(out));
        return MDRResult<RetVptCapability>::Success(std::move(out));
    }
    MDRResult<size_t> RetVptCapability_VptCapability::Serialize(const RetVptCapability_VptCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetVptCapability_VptCapabilityBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRArray<VptPreset>::Write)(data.vptPresets, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetVptCapability_VptCapability> RetVptCapability_VptCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetVptCapability_VptCapability out{};
        MDR_TRY_SIZE(RetVptCapability_VptCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetVptCapability_VptCapability, (RetVptCapability_VptCapabilityBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetVptCapability_VptCapability, (MDRArray<VptPreset>::Read)(&data, out.vptPresets, maxSize));
        MDR_TRY(RetVptCapability_VptCapability, Validate(out));
        return MDRResult<RetVptCapability_VptCapability>::Success(std::move(out));
    }
    MDRResult<size_t> SetEqEbbParamRetEqEbbCapability_EqCapability::Serialize(const SetEqEbbParamRetEqEbbCapability_EqCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.band, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.step, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRArray<EqPreset>::Write)(data.presetList, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetEqEbbParamRetEqEbbCapability_EqCapability> SetEqEbbParamRetEqEbbCapability_EqCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetEqEbbParamRetEqEbbCapability_EqCapability out{};
        MDR_TRY_SIZE(SetEqEbbParamRetEqEbbCapability_EqCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetEqEbbParamRetEqEbbCapability_EqCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetEqEbbParamRetEqEbbCapability_EqCapability, MDRPod::Read(&data, out.band, maxSize));
        MDR_TRY_SIZE(SetEqEbbParamRetEqEbbCapability_EqCapability, MDRPod::Read(&data, out.step, maxSize));
        MDR_TRY_SIZE(SetEqEbbParamRetEqEbbCapability_EqCapability, (MDRArray<EqPreset>::Read)(&data, out.presetList, maxSize));
        MDR_TRY(SetEqEbbParamRetEqEbbCapability_EqCapability, Validate(out));
        return MDRResult<SetEqEbbParamRetEqEbbCapability_EqCapability>::Success(std::move(out));
    }
    MDRResult<size_t> SetGsParamGsBooleanTypeValue::Serialize(const SetGsParamGsBooleanTypeValue& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (GsBooleanTypeValue::Write)(data.settingValue, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetGsParamGsBooleanTypeValue> SetGsParamGsBooleanTypeValue::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetGsParamGsBooleanTypeValue out{};
        MDR_TRY_SIZE(SetGsParamGsBooleanTypeValue, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetGsParamGsBooleanTypeValue, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetGsParamGsBooleanTypeValue, MDRPod::Read(&data, out.settingType, maxSize));
        MDR_TRY_SIZE(SetGsParamGsBooleanTypeValue, (GsBooleanTypeValue::Read)(&data, out.settingValue, maxSize));
        MDR_TRY(SetGsParamGsBooleanTypeValue, Validate(out));
        return MDRResult<SetGsParamGsBooleanTypeValue>::Success(std::move(out));
    }
    MDRResult<size_t> SetGsParamGsListTypeValue::Serialize(const SetGsParamGsListTypeValue& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (GsListTypeValue::Write)(data.currentElementIndex, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetGsParamGsListTypeValue> SetGsParamGsListTypeValue::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetGsParamGsListTypeValue out{};
        MDR_TRY_SIZE(SetGsParamGsListTypeValue, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetGsParamGsListTypeValue, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetGsParamGsListTypeValue, MDRPod::Read(&data, out.settingType, maxSize));
        MDR_TRY_SIZE(SetGsParamGsListTypeValue, (GsListTypeValue::Read)(&data, out.currentElementIndex, maxSize));
        MDR_TRY(SetGsParamGsListTypeValue, Validate(out));
        return MDRResult<SetGsParamGsListTypeValue>::Success(std::move(out));
    }
    MDRResult<size_t> SetNcAsmParamRetNcAsmCapability_AsmCapability::Serialize(const SetNcAsmParamRetNcAsmCapability_AsmCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<Asm>::Write)(data.asmList, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetNcAsmParamRetNcAsmCapability_AsmCapability> SetNcAsmParamRetNcAsmCapability_AsmCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetNcAsmParamRetNcAsmCapability_AsmCapability out{};
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_AsmCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_AsmCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_AsmCapability, MDRPod::Read(&data, out.asmSettingType, maxSize));
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_AsmCapability, (MDRPodArray<Asm>::Read)(&data, out.asmList, maxSize));
        MDR_TRY(SetNcAsmParamRetNcAsmCapability_AsmCapability, Validate(out));
        return MDRResult<SetNcAsmParamRetNcAsmCapability_AsmCapability>::Success(std::move(out));
    }
    MDRResult<size_t> SetNcAsmParamRetNcAsmCapability_NcAsmCapability::Serialize(const SetNcAsmParamRetNcAsmCapability_NcAsmCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncStep, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmSettingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRPodArray<Asm>::Write)(data.asmList, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetNcAsmParamRetNcAsmCapability_NcAsmCapability> SetNcAsmParamRetNcAsmCapability_NcAsmCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetNcAsmParamRetNcAsmCapability_NcAsmCapability out{};
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_NcAsmCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_NcAsmCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_NcAsmCapability, MDRPod::Read(&data, out.ncSettingType, maxSize));
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_NcAsmCapability, MDRPod::Read(&data, out.ncStep, maxSize));
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_NcAsmCapability, MDRPod::Read(&data, out.asmSettingType, maxSize));
        MDR_TRY_SIZE(SetNcAsmParamRetNcAsmCapability_NcAsmCapability, (MDRPodArray<Asm>::Read)(&data, out.asmList, maxSize));
        MDR_TRY(SetNcAsmParamRetNcAsmCapability_NcAsmCapability, Validate(out));
        return MDRResult<SetNcAsmParamRetNcAsmCapability_NcAsmCapability>::Success(std::move(out));
    }
    MDRResult<size_t> SetPlayParamPlaybackControllerNameData::Serialize(const SetPlayParamPlaybackControllerNameData& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.dataType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (PlaybackName::Write)(data.playbackName, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetPlayParamPlaybackControllerNameData> SetPlayParamPlaybackControllerNameData::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetPlayParamPlaybackControllerNameData out{};
        MDR_TRY_SIZE(SetPlayParamPlaybackControllerNameData, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetPlayParamPlaybackControllerNameData, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetPlayParamPlaybackControllerNameData, MDRPod::Read(&data, out.dataType, maxSize));
        MDR_TRY_SIZE(SetPlayParamPlaybackControllerNameData, (PlaybackName::Read)(&data, out.playbackName, maxSize));
        MDR_TRY(SetPlayParamPlaybackControllerNameData, Validate(out));
        return MDRResult<SetPlayParamPlaybackControllerNameData>::Success(std::move(out));
    }
    MDRResult<size_t> SetSportsParam::Serialize(const SetSportsParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeParam::Write)(data.trainingParam, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSportsParam> SetSportsParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSportsParam out{};
        MDR_TRY_SIZE(SetSportsParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSportsParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSportsParam, (TrainingModeParam::Read)(&data, out.trainingParam, maxSize));
        MDR_TRY(SetSportsParam, Validate(out));
        return MDRResult<SetSportsParam>::Success(std::move(out));
    }
    MDRResult<size_t> SetVptParamRetVptCapability_VptCapability::Serialize(const SetVptParamRetVptCapability_VptCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (MDRArray<VptPreset>::Write)(data.vptPresets, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetVptParamRetVptCapability_VptCapability> SetVptParamRetVptCapability_VptCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetVptParamRetVptCapability_VptCapability out{};
        MDR_TRY_SIZE(SetVptParamRetVptCapability_VptCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetVptParamRetVptCapability_VptCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetVptParamRetVptCapability_VptCapability, (MDRArray<VptPreset>::Read)(&data, out.vptPresets, maxSize));
        MDR_TRY(SetVptParamRetVptCapability_VptCapability, Validate(out));
        return MDRResult<SetVptParamRetVptCapability_VptCapability>::Success(std::move(out));
    }
    MDRResult<size_t> TestCommand::Serialize(const TestCommand& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.testCommandType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.sender, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.receiver, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.majorVersion, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.minorVersion, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (AtCommandParam::Write)(data.atCommandParam, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<TestCommand> TestCommand::Deserialize(const UInt8* data, size_t maxSize)
    {
        TestCommand out{};
        MDR_TRY_SIZE(TestCommand, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(TestCommand, MDRPod::Read(&data, out.testCommandType, maxSize));
        MDR_TRY_SIZE(TestCommand, MDRPod::Read(&data, out.sender, maxSize));
        MDR_TRY_SIZE(TestCommand, MDRPod::Read(&data, out.receiver, maxSize));
        MDR_TRY_SIZE(TestCommand, MDRPod::Read(&data, out.majorVersion, maxSize));
        MDR_TRY_SIZE(TestCommand, MDRPod::Read(&data, out.minorVersion, maxSize));
        MDR_TRY_SIZE(TestCommand, (AtCommandParam::Read)(&data, out.atCommandParam, maxSize));
        MDR_TRY(TestCommand, Validate(out));
        return MDRResult<TestCommand>::Success(std::move(out));
    }
    MDRResult<size_t> TrainingModeExParam::Write(const TrainingModeExParam& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.parameterType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmEffect, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.ncAsmSettingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.value4, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmSettingType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.asmId, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.value7, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExNcAsmParam::Write)(data.ncAsmParam, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExAsmParam::Write)(data.asmParam, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExEqParam::Write)(data.eqParam, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> TrainingModeExParam::Read(const UInt8** ppSrcBuffer, TrainingModeExParam& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.parameterType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncAsmEffect, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.ncAsmSettingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.value4, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmSettingType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.asmId, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.value7, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExNcAsmParam::Read)(ppSrcBuffer, out.ncAsmParam, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExAsmParam::Read)(ppSrcBuffer, out.asmParam, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExEqParam::Read)(ppSrcBuffer, out.eqParam, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> AsCapabilityKey::Write(const AsCapabilityKey& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.key, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.keyType, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.defaultPreset, ppDstBuffer, maxSize));
        MDR_TRY_SIZE(size_t, (MDRArray<AsCapabilityPreset>::Write)(data.presets, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> AsCapabilityKey::Read(const UInt8** ppSrcBuffer, AsCapabilityKey& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.key, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.keyType, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Read(ppSrcBuffer, out.defaultPreset, maxSize));
        MDR_TRY_SIZE(size_t, (MDRArray<AsCapabilityPreset>::Read)(ppSrcBuffer, out.presets, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RetGsCapabilityGsCandidateElementList::Serialize(const RetGsCapabilityGsCandidateElementList& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (GsSettingInfo::Write)(data.title, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.settingType, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (GsCandidateElementList::Write)(data.listTypeCapability, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetGsCapabilityGsCandidateElementList> RetGsCapabilityGsCandidateElementList::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetGsCapabilityGsCandidateElementList out{};
        MDR_TRY_SIZE(RetGsCapabilityGsCandidateElementList, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetGsCapabilityGsCandidateElementList, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(RetGsCapabilityGsCandidateElementList, (GsSettingInfo::Read)(&data, out.title, maxSize));
        MDR_TRY_SIZE(RetGsCapabilityGsCandidateElementList, MDRPod::Read(&data, out.settingType, maxSize));
        MDR_TRY_SIZE(RetGsCapabilityGsCandidateElementList, (GsCandidateElementList::Read)(&data, out.listTypeCapability, maxSize));
        MDR_TRY(RetGsCapabilityGsCandidateElementList, Validate(out));
        return MDRResult<RetGsCapabilityGsCandidateElementList>::Success(std::move(out));
    }
    MDRResult<size_t> SetSportsExParam::Serialize(const SetSportsExParam& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (TrainingModeExParam::Write)(data.trainingModeExParam, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSportsExParam> SetSportsExParam::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSportsExParam out{};
        MDR_TRY_SIZE(SetSportsExParam, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSportsExParam, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSportsExParam, (TrainingModeExParam::Read)(&data, out.trainingModeExParam, maxSize));
        MDR_TRY(SetSportsExParam, Validate(out));
        return MDRResult<SetSportsExParam>::Success(std::move(out));
    }
    MDRResult<size_t> AsCapabilityKeyList::Write(const AsCapabilityKeyList& data, UInt8** ppDstBuffer, size_t maxSize)
    {
        UInt8* ptr = *ppDstBuffer;
        MDR_TRY_SIZE(size_t, (MDRArray<AsCapabilityKey>::Write)(data.elements, ppDstBuffer, maxSize));
        return MDRResult<size_t>::Success(*ppDstBuffer - ptr);
    }
    MDRResult<size_t> AsCapabilityKeyList::Read(const UInt8** ppSrcBuffer, AsCapabilityKeyList& out, size_t maxSize)
    {
        const UInt8* ptr = *ppSrcBuffer;
        MDR_TRY_SIZE(size_t, (MDRArray<AsCapabilityKey>::Read)(ppSrcBuffer, out.elements, maxSize));
        return MDRResult<size_t>::Success(*ppSrcBuffer - ptr);
    }
    MDRResult<size_t> RetSystemCapability_AssignableSettingsCapability::Serialize(const RetSystemCapability_AssignableSettingsCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (RetSystemCapability_SystemCapabilityBase::Write)(data.base, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (AsCapabilityKeyList::Write)(data.assignableSettingKeyList, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<RetSystemCapability_AssignableSettingsCapability> RetSystemCapability_AssignableSettingsCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        RetSystemCapability_AssignableSettingsCapability out{};
        MDR_TRY_SIZE(RetSystemCapability_AssignableSettingsCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_AssignableSettingsCapability, (RetSystemCapability_SystemCapabilityBase::Read)(&data, out.base, maxSize));
        MDR_TRY_SIZE(RetSystemCapability_AssignableSettingsCapability, (AsCapabilityKeyList::Read)(&data, out.assignableSettingKeyList, maxSize));
        MDR_TRY(RetSystemCapability_AssignableSettingsCapability, Validate(out));
        return MDRResult<RetSystemCapability_AssignableSettingsCapability>::Success(std::move(out));
    }
    MDRResult<size_t> SetSystemExParamRetSystemCapability_AssignableSettingsCapability::Serialize(const SetSystemExParamRetSystemCapability_AssignableSettingsCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (AsCapabilityKeyList::Write)(data.assignableSettingKeyList, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSystemExParamRetSystemCapability_AssignableSettingsCapability> SetSystemExParamRetSystemCapability_AssignableSettingsCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSystemExParamRetSystemCapability_AssignableSettingsCapability out{};
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_AssignableSettingsCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_AssignableSettingsCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSystemExParamRetSystemCapability_AssignableSettingsCapability, (AsCapabilityKeyList::Read)(&data, out.assignableSettingKeyList, maxSize));
        MDR_TRY(SetSystemExParamRetSystemCapability_AssignableSettingsCapability, Validate(out));
        return MDRResult<SetSystemExParamRetSystemCapability_AssignableSettingsCapability>::Success(std::move(out));
    }
    MDRResult<size_t> SetSystemParamRetSystemCapability_AssignableSettingsCapability::Serialize(const SetSystemParamRetSystemCapability_AssignableSettingsCapability& data, UInt8* out, size_t maxSize)
    {
        UInt8* ptr = out;
        MDR_TRY(size_t, Validate(data));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.command, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, MDRPod::Write(data.type, &ptr, maxSize));
        MDR_TRY_SIZE(size_t, (AsCapabilityKeyList::Write)(data.assignableSettingKeyList, &ptr, maxSize));
        return MDRResult<size_t>::Success(ptr - out);
    }
    MDRResult<SetSystemParamRetSystemCapability_AssignableSettingsCapability> SetSystemParamRetSystemCapability_AssignableSettingsCapability::Deserialize(const UInt8* data, size_t maxSize)
    {
        SetSystemParamRetSystemCapability_AssignableSettingsCapability out{};
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_AssignableSettingsCapability, MDRPod::Read(&data, out.command, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_AssignableSettingsCapability, MDRPod::Read(&data, out.type, maxSize));
        MDR_TRY_SIZE(SetSystemParamRetSystemCapability_AssignableSettingsCapability, (AsCapabilityKeyList::Read)(&data, out.assignableSettingKeyList, maxSize));
        MDR_TRY(SetSystemParamRetSystemCapability_AssignableSettingsCapability, Validate(out));
        return MDRResult<SetSystemParamRetSystemCapability_AssignableSettingsCapability>::Success(std::move(out));
    }
}
